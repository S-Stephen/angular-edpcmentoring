import { TestBed } from "@angular/core/testing";

import { NavMenuConfigService } from "./nav-menu-config.service";

describe("NavMenuConfigServiceService", () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  //it("should be created", () => {
  // typescript is complaining with regard to the tpyes not matching
  // as we are returning an injectToken
  //  const service: NavMenuConfigService = TestBed.get(NavMenuConfigService);
  //  expect(service).toBeTruthy();
  //});
});
