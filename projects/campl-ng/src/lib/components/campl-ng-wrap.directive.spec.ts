import { CamplNgWrapDirective } from './campl-ng-wrap.directive';

describe('CamplNgWrapDirective', () => {
  it('should create an instance', () => {
    const directive = new CamplNgWrapDirective();
    expect(directive).toBeTruthy();
  });
});
